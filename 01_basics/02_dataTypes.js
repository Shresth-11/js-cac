"use strict"; //treat all JS code as newer version

// alert(3+3) // we are using nodejs, not browser, since javascript engine resides in the browser, but we can use this feature (alert) with different method in node too.

let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value and dataType too, null is both.
// undefined => variable declared but value is not given.
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object




console.table([typeof undefined, typeof null, typeof isLoggedIn, typeof state, typeof name]);


